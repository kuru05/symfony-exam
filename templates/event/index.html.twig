{% extends 'base.html.twig' %}

{% block title %}Liste des événements{% endblock %}

{% block body %}
<div class="container py-5">
    <h1 class="mb-4">Prochains événements</h1>
    <div class="row g-4">
        {% for event in events %}
            <div class="col-md-4">
                <div class="card h-100 p-3">
                    <div class="card-body">
                        <h5 class="card-title">{{ event.title }}</h5>
                        <p class="card-text text-muted small">📅 {{ event.date|date('d/m/Y H:i') }} • 📍 {{ event.location }}</p>
                        <p class="card-text">{{ event.description|slice(0, 100) }}...</p>
                        <a href="{{ path('app_event_show', {'id': event.id}) }}" class="btn btn-sm btn-primary mt-3">Détails</a>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="col-12">
                <p>Aucun événement trouvé.</p>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
